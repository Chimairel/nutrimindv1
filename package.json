{
  "name": "nutrimind",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "audit": "npm audit && npm --prefix backend audit && npm --prefix frontend audit",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "architecture:check": "node scripts/check-source-architecture.mjs",
    "check": "npm run architecture:check && npm run format:check && npm run lint && npm run test && npm run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "docker:build": "docker compose build",
    "docker:config": "docker compose config",
    "docker:down": "docker compose down",
    "docker:up": "docker compose up --build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "install:all": "npm ci && npm --prefix backend ci && npm --prefix frontend ci",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "npm --prefix backend run lint",
    "lint:frontend": "npm --prefix frontend run lint",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --prefix backend test",
    "test:e2e": "npm --prefix frontend run test:e2e",
    "test:frontend": "npm --prefix frontend test"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  },
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11"
  }
}
